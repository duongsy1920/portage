# Kiến thức chung — go/ · ddd/ · agent/

**Rỗng lúc đầu, và chỉ đầy bằng một cách:** khi một dòng trong `../learning/debt.md`
được **gạch** — nghĩa là có một task thật đã trả nợ đó — thì bài trả nợ đi vào đây.

Không nhận bài viết trước. Không nhận bài chép từ tài liệu ngoài. Một bài ở đây phải
trỏ được tới task và `file:dòng` đã dạy nó. Kiến thức về **một** repo cụ thể không ở
đây — ở `../projects/<tên>/knowledge/`.

Nợ 30 ngày không ai đụng → routine `review` gợi xoá.
